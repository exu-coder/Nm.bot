# Farabi IT Center Admin

Vite + Supabase data dashboard with a demo client-side login gate.

## Configuration

Copy `.env.example` to `.env` and set the Base64-encoded admin email/password plus Supabase URL and anon key.

> The `VITE_*` values are shipped to the browser. Base64 is obfuscation, not encryption. Do not use this login as production authentication.
